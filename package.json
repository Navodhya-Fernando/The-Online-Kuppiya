{
  "name": "the-online-kuppiya",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix frontend run build",
    "start": "npm run build && node backend/server.js",
    "dev": "npm run build && node backend/server.js"
  }
}